/**
 * Shared request validators live here when they span multiple modules.
 * Module-specific validators stay inside each module's `validators/` folder.
 */

export const sharedValidationsReady = true;
